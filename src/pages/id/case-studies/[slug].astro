---
import Page, { getStaticPaths as originalGetStaticPaths } from "../../case-studies/[slug].astro";
export const getStaticPaths = originalGetStaticPaths;
const { ...props } = Astro.props;
---
<Page {...props} />

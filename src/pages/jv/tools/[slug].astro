---
import Page, { getStaticPaths as originalGetStaticPaths } from "../../tools/[slug].astro";
export const getStaticPaths = originalGetStaticPaths;
const { ...props } = Astro.props;
---
<Page {...props} />

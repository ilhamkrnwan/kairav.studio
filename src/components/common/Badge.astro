---
const { class: className = "" } = Astro.props;
---

<span
  class:list={[
    "inline-flex items-center rounded-full border border-[var(--color-border)] bg-[var(--color-surface-glass)] px-3 py-1 font-mono text-[0.68rem] font-medium uppercase text-[var(--color-primary)]",
    className,
  ]}
>
  <slot />
</span>
